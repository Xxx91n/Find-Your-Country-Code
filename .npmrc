# 票 31：依赖安装面修复（deviation D-31a，详见窗口报告）。
# react19/react-dom19 别名 devDeps（票 15 fixture）与 react@18 存在 peer 冲突，
# 周期 3 在 typecheck.yml 以 --legacy-peer-deps 内联处理；cch-25 依赖钉死后
# lockfile 未再生成，e2e.yml 裸 npm install ERESOLVE 红。集中到 .npmrc 统一生效。
legacy-peer-deps=true
