# 2026-09-18 报告 —— Cycle-8 T-01…T-08 落地（收口治理 + 证据闭环）

> **身份**：修复/开发子 Agent（常驻任务书执行窗口）
> **分支**：`cch/17-cycle8-grill`（GitButler，**未 `land`／未 `push`**）｜**基线**：`origin/main` = `d075f01a`（v1.7.0，本轮不发版）
> **任务书**：`.scratch/cycle8-grill/handoffs/next-round.md`｜**数据源**：`.scratch/cycle8-grill/decision-ledger.md`（D-001…D-007，7/7 current）
> **口径**：本报告每条声明均附**可复跑命令 + 输出摘要**；**不引入账本以外的结论**。

---

## 0. 结论

T-01…T-08 **全部落地**（覆盖 **D-004 · D-005 · D-006 · D-007**）。**T-09 未动**（账本无对应 D-xxx，须先经用户拍板）。
验收口径逐条闭环：**编译通过 · 打包通过 · 启动并测活 · 每个平台 test 闭环**。
**无任何既有门禁被降低**；票级门 **20/21**（唯一红 = 票 39 **基线预存红**，见 findings register FR-04）。

---

## 1. 覆盖与去向

| T | 覆盖 D | 交付物 | 状态 |
|---|---|---|---|
| **T-01** | D-004 | `.scratch/` 分区 `evidence/` + `draft/` ＋ 三层边界 README ＋ **9 个零引用探针**迁入 `draft/probes/` ＋ 清理脚本白名单化（`scratch-draft-clean.mjs`） | ✅ |
| **T-02** | D-004 | 留痕三件套规则落 README ＋ **22 件**现存外部输入补录 **SHA-256** 台账（含来源/入库日期/push 状态） | ✅ |
| **T-03** | D-005 | `git check-ignore -v` 防误吞门禁（`check-ignore-guard.mjs`）**折叠进既有 workflow**（`typecheck.yml`），不新增 workflow 文件 | ✅ |
| **T-04** | D-005 | 被忽略目录边界收口：`live-out/` 复核项**关闭**（保持忽略）· `docs/*` 定性「合法、不重写」· 外部输入禁落被忽略目录 | ✅ |
| **T-05** | D-007 | findings register：**FR-01…FR-07**（带到期日 2027-03-31）＋ FC-01…FC-08 ＋ FN-01/FN-02 | ✅ |
| **T-06** | D-006 B⑧ | ADR-0005 `## 二次裁决的量化门槛` → Notes 下 `###`（**内容逐字保留**）＋ 头部 `date` 更新 | ✅ |
| **T-07** | D-006 B⑩ | ADR-0008 追加带日期 Notes（`verdict`↔`expect` 联动语义 ＋ 机器校验 `32-real-site-corpus.mjs:193–201`） | ✅ |
| **T-08** | D-006 C⑭ | LF 清账 **121 文件**（工作区 CRLF → **0**，**零内容差异**）＋ 新增 `.editorconfig` | ✅ |
| **T-09** | （无） | **未执行** —— 账本无对应 D-xxx，须先经用户拍板 | ⏸ |

---

## 2. 变更清单（文件级）

**新增（10）**

| 文件 | 任务 |
|---|---|
| `.scratch/README.md` | T-01 受管区边界图（分区/判据/白名单/留痕/CI 消费面/忽略边界/防误吞/登记册） |
| `.scratch/evidence/README.md` · `.scratch/draft/README.md` | T-01 两区边界 |
| `.scratch/evidence/external-inputs-ledger.md` | T-02 外部输入哈希台账（22 行，脚本生成非转录） |
| `.scratch/evidence/findings-register.md` | T-05 findings register |
| `.scratch/draft/probes/*.mjs`（9） | T-01 零引用探针迁入草稿区 |
| `tests/scripts/check-ignore-guard.mjs` | T-03 防误吞门禁（含自检） |
| `tests/scripts/scratch-draft-clean.mjs` | T-01 白名单清理脚本（含自检） |
| `.editorconfig` | T-08 行尾防再漂移 |

**修改（5）**

| 文件 | 任务 |
|---|---|
| `.github/workflows/typecheck.yml` | T-03 折叠防误吞门禁 step（新增 1 step，不新增文件） |
| `.github/workflows/engine-gates.yml` | T-01 折叠清理脚本白名单自检 step |
| `docs/adr/0005-pseudo-select-recognition-implement.md` | T-06 层级整理 + 头部 date |
| `docs/adr/0008-real-site-testing-layers.md` | T-07 带日期 Notes |
| `docs/adr/0013-governed-artifact-area-and-evidence-trail.md` | T-04 带日期 Notes（关闭 `live-out/` 复核项） |

**迁移（9，`D` + `R`）**：`.scratch/architecture-recovery/research/scripts/{08-probe-dataset,39-candidates-probe,39-codepen-probe,39-headed-probe,39-inject-probe,39-matrix-probe,39-matrix2,39-selector-probe,39-viewport-probe}.mjs` → `.scratch/draft/probes/`

**提交**（`but`，未 land／未 push）：

| commit | sha | 内容 |
|---|---|---|
| `xxn` | `5d064ab8` | T-01/T-02/T-05 —— 受管区落地 |
| `yrs` | `7b98d7fc` | T-03/T-04/T-06/T-07/T-08 —— 门禁 + ADR + 行尾 |

---

## 3. 验收标准逐条闭环

| 验收项（原文） | 闭环证据 |
|---|---|
| **编译通过** | `npm run typecheck` → **exit 0**（`tsc --noEmit`） |
| **打包通过** | `npm run build` → exit 0；`dist/find-your-country-code.user.js` = **171,172 B**；两次构建 sha256 **完全一致** |
| **启动并测活软件进程** | ① 显式探针：`node tests/server.mjs` → 进程存活（`kill -0` yes）· `GET /test/test-page.html` → **HTTP 200**（2,745 B）· 停进程后 `kill -0` no；② 全链路：`npm run e2e` → **149 passed / 0 failed**（webServer 进程 + Chromium，含 L0/L1/L2/L3/L4 断言） |
| **每个平台都要有 test 闭环** | 见 §3.1（逐平台清单） |

### 3.1 平台 × test 闭环清单

| 平台 / 测试面 | 本地闭环命令 | CI 挂接点 | 本次结果 |
|---|---|---|---|
| Node 引擎与语料（零浏览器） | `verify-ticket-runner.mjs --run <t>`（21 张票级门） | `verify-tickets.yml`（matrix） | **20 pass / 1 fail**（唯一红 = 票 39 基线预存） |
| Node 引擎门 | `verify-ticket-02.mjs` · `misdetect-repro-v2.mjs` | `engine-gates.yml` | 36/36 + G10 5/5 |
| 校准语料（precision/recall） | `14-calibration-harness.mjs` | `calibration-baseline.yml` | cases=63 TP=31 FP=0 TN=32 FN=0 precision=1 recall=1 f1=1 **gate=pass** |
| 真实站点语料探针 | `32-real-site-corpus.mjs` | `calibration-baseline.yml` | forms=3 scanSelectors=13 selfTest=pass **gate=pass** violations=0 |
| Chromium E2E | `npm run e2e` | `e2e.yml` | **149 passed / 0 failed**（1.6m） |
| 类型 + 文档事实 + 防误吞 | `npm run typecheck` · `doc-facts.mjs` · `check-ignore-guard.mjs` | `typecheck.yml` | typecheck 0；doc-facts OK（23 selectors / 4 menu commands）；guard OK（24 关键路径 + 5 阴性对照） |
| 仓库治理门 | `issue-checkbox-audit.mjs` · `corpus-exemption-lint.mjs` · `scratch-draft-clean.mjs --self-test` | `engine-gates.yml` | PASS / 7-0 / OK |
| 真实站点冒烟（第三方站点） | `real-site-smoke.yml`（schedule + dispatch） | `real-site-smoke.yml` | **不适用**：ADR-0008 决策 1 硬约束「永不进入 `pull_request` 触发面」且需外网供给；本轮**未改其触发面**（改即违反决策） |

> **说明**：最后一项不是「只引入却没做到」，而是 **ADR-0008 决策 1 的显式设计**（低频弱断言 + advisory）。本轮未触碰它，也未削弱它。

---

## 4. 逐任务证据表（命令 + 输出摘要）

### T-01 分区 + 白名单清理

```
$ node tests/scripts/scratch-draft-clean.mjs --self-test
scratch-draft-clean: whitelist = .scratch/draft/  |  mode = dry-run (no deletion)
  candidates: 9 file(s), 0 empty dir(s)
    - .scratch/draft/probes/08-probe-dataset.mjs
    - .scratch/draft/probes/39-candidates-probe.mjs
    ... (共 9)
  PASS  self-test (whitelist accept/refuse + traversal refusal + dry-run purity + README protected)
scratch-draft-clean: OK        EXIT=0
```
自检覆盖：区外路径拒绝（`.scratch/README.md` / `evidence/` / `architecture-recovery/issues/` / `docs/adr/` / 仓库根）、`../` 越权穿越拒绝（2 例）、dry-run 纯度、`draft/README.md` 保护。
**迁移判据（可复跑）**：全仓 505 个文本文件中，9 个探针的文件名/主干**引用数 = 0**，且不被任何 CI 门禁读取。

### T-02 留痕三件套

```
$ node -e "..."  # 脚本遍历仓库实际文件并计算 SHA-256，生成台账
wrote 8117 bytes, lines=57, BOM=false, CRLF=false
rows=22; pushedInMain=20
```
台账覆盖：atomcode 产出 10 · prompt 存档 5 · 审计报告 2 · grill 调研存档 5。**未闭合**：2 件仅在本会话分支（未 push）；锐评原文不可恢复（无哈希可录）。

### T-03 防误吞门禁

```
$ node tests/scripts/check-ignore-guard.mjs --self-test
check-ignore-guard: key paths = 24, negative controls = 5
  PASS  not-ignored  .scratch/README.md
  PASS  not-ignored  .scratch/evidence/findings-register.md
  ... (共 24)
  PASS  ignored      dist/  (.gitignore:22:dist/)
  PASS  ignored      test-results/  (.gitignore:25:test-results/)
  PASS  ignored      playwright-report/  (.gitignore:26:playwright-report/)
  PASS  ignored      live-out/  (.gitignore:29:live-out/)
  PASS  ignored      node_modules/  (.gitignore:21:node_modules/)
  PASS  self-test (parser + non-vacuous inventory + probe channel)
check-ignore-guard: OK (24 key paths not ignored, 5 negative controls ignored)   EXIT=0
```
**双向断言**（不是恒真）：正向断言关键路径不被吞；**阴性对照**断言可抛弃目录确实被忽略，证明探测通道真的能区分。

```
$ python -c "yaml.safe_load(...)"
.github/workflows/typecheck.yml: OK  jobs=['typecheck']  steps=7
    ... - Doc facts (ACCEPTANCE-SURFACE vs src)
    - Ignore-swallow guard (key paths must not be gitignored)
.github/workflows/engine-gates.yml: OK  jobs=['engine-gates']  steps=6
    ... - Draft cleanup whitelist self-test (scratch-draft-clean.mjs --self-test)
```

### T-04 被忽略目录边界

```
$ grep -rn 'live-out' --include=*.mjs --include=*.ts --include=*.yml --include=*.json tests .github src
.github/workflows/real-site-smoke.yml:75:  --json live-out/smoke-summary.json \
.github/workflows/real-site-smoke.yml:76:  --out live-out/smoke-report.md
.github/workflows/real-site-smoke.yml:82:  ... --json live-out/cdp-fitness.json
.github/workflows/real-site-smoke.yml:115:  path: live-out/
```
**读取方 = 0**，仅写入/上传。⇒ `live-out/` 为纯诊断产物，**不晋升入库**；ADR-0013 反证条件 3 相应关闭（已落带日期 Notes）。

### T-05 findings register

`FR-01…FR-07` 均具齐五要素（发现 ID · 理由 · 补偿性控制 · 具名签核 · **到期日 2027-03-31，绝不自动续期**）；其中 4 条签核栏为 **「待用户签核」**（未自行代签）。

### T-06 ADR-0005 层级整理（内容逐字保留）

```
断言（脚本内）：
  ok  ADR-0005 no longer has the stray H2
  ok  ADR-0005 has the ### subsection
  ok  ADR-0005 header date updated
  ok  criterion 1 verbatim / criterion 2 verbatim / criterion 3 verbatim / closing line verbatim
  ok  ADR-0005 MADR H2 order restored（背景→决策→依据→反证条件→后果→Notes）
```

### T-07 ADR-0008 联动语义登记

```
  ok  ADR-0008 notes landed
  ok  ADR-0008 cites the machine check（32-real-site-corpus.mjs **:193–201**）
  ok  ADR-0008 Notes before 参考
```
**不新立 ADR**；不新开决策；仅把既有联动语义（`MISS→expect=inject/knownResidual=true`、`FIXED→inject/false`、`COVERED→none/false`）与**已存在的机器校验**落文。

### T-08 LF 清账 + `.editorconfig`

```
BEFORE: worktree CRLF files = 121
BEFORE: blob already LF = 121 | blob CRLF = 0      ← 证实 T-08 完成判据「blob 已全 LF」
AFTER : worktree CRLF files = 0
AFTER : BOM files = 0
```
最终校验（全仓 tracked 文本，排除二进制）：`worktree CRLF 文件数: 0` · `BOM 文件数: 0` · `git diff --check: clean`。

---

## 5. 隔离重建对照（审计纪律：不以脏工作区冒充提交态）

```
$ TMP=$(mktemp -d); git archive HEAD | tar -x -C "$TMP"; ...
isolated build:        size=171172 sha256=c324c47a481e2e788d803ec305033289e119015885d4abc143eba5e697d863a1
worktree build:        size=171172 sha256=c324c47a481e2e788d803ec305033289e119015885d4abc143eba5e697d863a1
isolated calibration:  cases=63 TP=31 TN=32 gate=pass
isolated real-site:    forms=3 scanSelectors=13 gate=pass
isolated AUDIT:        19 passed / 0 failed
```
⇒ **提交态 == 工作区态，逐字节一致**（build 产物、语料指标、票级审计三项全同）。
另：本产物 sha256 `c324c47a481e…` 与账本 D-002 窗口记录**完全相同** ⇒ 本轮**零构建回归**。

---

## 6. 过程发现（已闭环，详见 findings register §六）

| ID | 发现 | 为何重要 | 处置 |
|---|---|---|---|
| **FN-01** | 本机 **bash `/tmp` = `%TEMP%`，而 Node `/tmp` = `D:\tmp`** —— 「脚本写 `/tmp/x.json`、Node 读 `/tmp/x.json`」会读到**另一个文件**。本轮曾因此读到**昨日残留**（mtime `2026-09-17T13:16:23.930Z`），得出错误的 `cases=56 / scanSelectors=10` | 可让人**误判「改动引入回归」**；本轮靠隔离重建才揭穿 | 改用无歧义绝对路径（`D:/tmp/...`）；建议写回 `WORKFLOW §2`（待拍板） |
| **FN-02** | LF 清账后 git 与 GitButler 均报 **121 条 `M`**，但**内容零差异**（`hash-object` == index blob == HEAD blob；`git diff --numstat` 空；`but diff` 报 “No diff available”） | 工作区噪声会污染提交流 | 用 **`but discard`**（`but` 唯一入口）分 3 批清 **120** 条；清除后逐路径校验 **contentChanged=0 / missing=0**；最终 `git status --porcelain` = **0 行** |

---

## 7. 残留与未闭合

| 项 | 状态 | 责任 |
|---|---|---|
| **FR-01…FR-07**（登记册） | open，到期日 **2027-03-31** | 4 条待用户签核 |
| **T-09 四问**（Q3/Q5/Q6/Q7） | **未拍板**，未立票 | 用户 |
| 备份 ref `refs/backup/main-pre-rewrite` | 未清（**破坏性，待明确确认**） | 用户 |
| 本轮 **未 push ⇒ 无 CI run** | 全部门禁证据为**本地输出** | 待授权 |
| `38-gf-alignment-check.mjs` | 本轮复测得 **INCONCLUSIVE（网络不可达，非版本判定失败）**（早前同门曾为 OK；其信号源为第三方站点实时状态，属 ADR-0006 monitor 类） | 已知 |
| 票 39 基线预存红 | 维持不修（审计 §2「不能动」） | FR-04 |
| `WORKFLOW §2` 增补 `/tmp` 映射条款 | **未落**（涉纪律文本，未获授权） | 用户 |

---

## 8. 硬边界遵守声明

| 边界 | 实际 |
|---|---|
| 版本控制唯一入口 `but` | ✅ 全部提交/清除均用 `but`；**未执行任何裸 `git` 写操作**（含 `git add`/`commit`/`push`） |
| 未获授权不得 `land`／`push` | ✅ **未 land、未 push** |
| 本轮不发版（ADR-0010 不触发） | ✅ `package.json` 仍 `1.7.0`，未动 |
| 不得降低任何既有门禁 | ✅ 零门禁降级；新增门禁只增不减；票级门仍 20/21（与基线同） |
| 门禁证据只认 CI run/artifact | ⚠️ 本轮无 CI run（未 push）⇒ 证据均为**本地同口径复跑**，已如实标注 |
| 语料先行 / 禁物理删除语料条目 | ✅ `tests/corpus/manifest.json` 未动（sha 全程不变） |
| 性能红线 1000 节点 < 350ms | ✅ E2E 含 perf-fingerprint 用例，149 passed |
| 不引入远程网络面 / ML | ✅ `src/` 未动（`git status` 可验） |
| `.scratch/` 为受管区，清理只触 `draft/` | ✅ 清理脚本硬白名单；本轮**未删除任何内容**（只迁移） |
| 账本不得置于被忽略目录 | ✅ 防误吞门禁逐路径断言 |
| 4 个反向断言门不得削弱 | ✅ `grep -rl '\\.scratch/' .github/workflows/` → **0**；verify-07/10/11/12 均绿 |
| force-push 例外仅此一次 | ✅ 本轮未再使用；未据为先例 |

---

## 9. 复跑清单（最小集）

```bash
cd "D:/Aworker/mozilla/choose-your-country"

# 验收四件套
npm run typecheck
timeout 120 npm run build && sha256sum dist/find-your-country-code.user.js
node tests/server.mjs & sleep 3; curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:4273/test/test-page.html; kill %1
npm run e2e

# 门禁总账
node tests/scripts/verify-ticket-runner.mjs --audit
for t in $(node tests/scripts/verify-ticket-runner.mjs --list); do node tests/scripts/verify-ticket-runner.mjs --run "$t"; done
node tests/scripts/14-calibration-harness.mjs --out D:/tmp/c.md --json D:/tmp/c.json
node tests/scripts/32-real-site-corpus.mjs --out D:/tmp/r.md --json D:/tmp/r.json
node tests/scripts/doc-facts.mjs
node tests/scripts/issue-checkbox-audit.mjs
node tests/scripts/corpus-exemption-lint.mjs
node tests/scripts/38-gf-alignment-check.mjs
node tests/scripts/verify-ticket-02.mjs

# 本轮新增门禁
node tests/scripts/check-ignore-guard.mjs --self-test
node tests/scripts/scratch-draft-clean.mjs --self-test

# 卫生
git diff --check
git ls-files | grep -vE '\\.(bat|ps1|cmd|png|ico|icns|jpg|jpeg|gif|webp|exe|dll|node|pdb|msi|so|dylib|bin|zip|gz|tar|7z)$' | xargs -r grep -lU "$(printf '\\r')"   # 期望：无输出
```

> **注**：所有产物输出路径请用**绝对路径**（如 `D:/tmp/...`）—— 见 FN-01。
