# Decision Ledger — Cycle-6 Grill（逐题台账）

> 用途：grill 过程中**每一条被用户确认的实质性结论**当场落盘，不依赖对话记忆。

> 生成：2026-09-15 | 基线：origin/main = 85990d2f（v1.6.0）

> 上游：`.scratch/architecture-recovery/research/cycle6-investigation.md`（Cycle-6 架构大脑调查）+ `handoffs/47-cycle6-brain.md`

> 机制：ID 自 D-001 起递增；每条含【原问题 / 用户原回答原文 / 规范化需求 / 显式约束与负向需求 / 状态】。

> 状态取值：`current`（已确认生效）/ `pending`（**调研结论已出、待用户拍板，未生效**——用户指令要求冲突时不静默改向）/ `revised`（被后续回答修订）/ `stale`（被证伪或废弃）/ `deferred`（登记在案，本周期不做）。

> 硬规则：**结论不许只活在对话里**；触发任何压缩/compact/handoff 动作前，先确认本台账已落盘到最新。

---

## 台账

| ID | 原问题 | 用户原回答原文 | 规范化需求 | 显式约束 / 负向需求 | 状态 |
|----|--------|---------------|-----------|--------------------|------|
| **D-001** | Q1 — 本轮要交付的是「测试能证明工具生效」，还是「工具确实在指定页面上生效」？+ 「特殊指定网页」在你心里是哪种页面（点名真实站 / 代表性样本 / 自有可控页）？ | `C` / `指定网页，你去爬找吧，我这没资源` | 本周期交付**分两阶段、同一周期内先后完成**：（A）先建「在指定页面上验证全部工具」的**验证能力**（harness 交互原语 + 断言阶梯 + 页面语料）；（B）再用其跑出的**失效清单驱动修复**。指定页面的**选取与维护权委托给 Agent**——由 Agent 联网调研/爬取自行确定。 | **用户不提供具体站点清单，也不提供页面资源（原话「我这没资源」）——不得向用户索要站点列表；页面来源必须由 Agent 自行调研取得。** 排除 (A)-only（只建测试不修）与 (B)-only（无取证面直接修）。 | implemented（两阶段已交：阶段 A 票 01-07 建验证能力；阶段 B 票 08 失效驱动修复；页面选取权由 Agent 行使，未向用户索要清单） |
| **D-002** | Q2(2) — 「工具生效」由谁判定：页面侧外部可观测为准，还是脚本自报为准？ | （调研结论，非用户原话）用户指令：`将当前问题Q2完整提交 atomcode-research 深度调研……给出推荐与理由。调研的结果辩证性看待` | **验收裁定权在页面侧外部可观测结果**（DOM 变化、字段 `value` + `input/change` 事件、面板可见性、异常计数）；**脚本自报（`__cchLastFill`、toast 文案、signals）只进日志/诊断面板，不进断言**。同时保留「双轨不一致即缺陷」的不一致检测作诊断判据。 | **不得以内部状态作页面级验收依据**（上报者与被验收者同源，无独立性）；**不得因该结论而删掉引擎单元层的信号/分数断言**（引擎单元门仍是合法证据，只是不得充当「页面级生效」的证据）；不得因该结论而停建诊断面（诊断面正是内部状态的正确用途）。 | implemented（tests/ACCEPTANCE-SURFACE.md §1 裁定原则；票 07 真实站点层按页面侧外部可观测裁定） |
| **D-003** | Q2(3) — 断言深度到哪一级、各层归属如何？**（本条与 A-029 / A-030 冲突，已触发禁静默改向）** | （调研结论，非用户原话）同上用户指令 | **阶梯重定为五级**：L0 静默健康（pageerror=0，**永不单独算生效**）/ L1 元素已注入 / L2 交互可驱动（可见状态迁移）/ **L3 写入结果正确 + 持久化（自动化验收的最强推荐位）** / L4 用户反馈出现。**归属**：密封/自有页跑 L0–L3 全量（PR 阻断）；**真实站点只跑 L0–L1（+观察 L2）**；**L4 不自动化**，由发布前人工 checklist + 生产遥测兜底。环境真实性：L2 以上必须在真实浏览器 runtime；headless 必须 **full Chromium**（headless_shell 加载不了扩展，会静默空转全绿）。 | **自动化不得假言 L4（用户反馈出现）**；真实站点层不得承担写入结果正确（L3）的断言；不得用固定 sleep 等待（须可见状态迁移）；不得把「注入存在」当「能用」；跨隔离上下文链路必须覆盖（单上下文枚举必漏）。 | revised → implemented（五级阶梯 / full Chromium / 跨上下文必覆盖 / L3 为最强自动化位 均保留生效；「L4 不自动化」与「真实站点止于 L0-L1」两条被 D-004 覆盖） |
| **D-004** | 请你拍板（三件）：D-002 / D-003 / A-029·A-030 的 revised 是否确认？ | `接受；自动化吧还是；我觉得还得真实站点测` | **（a）** D-002 接受（验收裁定权在页面侧外部可观测；自报只作诊断）；**（b）L4（用户反馈出现）纳入自动化断言面**——覆盖调研的「不应让自动化假言 L4」建议；**（c）真实站点必须纳入测试面**——不得只停留在 L0–L1 弱断言；**（d）** A-029 / A-030 原口径**恢复生效**（真实站点断言口径提升 + owned 页全阶梯 + PR 门）。 | **不得以「调研建议」为由把 L4 排除出自动化；不得以「第三方不可控」为由把真实站点降为只观测。** 同时：**L4 自动化必须建立在外部可观测之上（D-002 不变），不得用脚本自报冒充反馈**；真实站点深断言的固有 flaky 必须由分层治理承担（advisory 触发面隔离 / soft assert 一次收全量 / skip 必带 reason+ticket），**不得让真实站点 flaky 污染密封层或阻断 PR（除非用户另行裁决）**；不得用固定 sleep 等待。 | implemented（L4 入自动化：票 01 §4.2 层归属 + 票 07 真实站点 L0-L4 实跑；flaky 由分层治理承担） |
| **D-005** | Q3 — 真实站点层失败时，是否阻断合入？+ 真实站点层的页面允许「已知不可达/被挑战」条目以 `observe` 长期挂账吗？ | `走你推荐的吧` | **（A）真实站点层保持 advisory 触发面隔离 + 新增「发布门」**：真实站点层按**全阶梯（含 L4）**运行，但仅由 `schedule`（周）+ `workflow_dispatch` 触发，**不进 `pull_request`**，失败只告警不阻断合入；**另在 `release.yml` 加发布门：真实站点层最近一次运行必须为绿，或失败已被显式 ack 并立票，否则不出包**。**并且**：允许已知不可达/被挑战的条目以 `observe` 形式长期挂账（只记录不裁定），**强制携带非空 reason + ticket**。 | **真实站点层不得进入 `pull_request` 触发面（ADR-0008 第二层不重开）；不得让真实站点 flaky 阻断合入；发布门不得被静默绕过（必须「绿」或「显式 ack + 立票」二选一）；`observe` 挂账必须带非空 reason + ticket（不得匿名挂账）。** 深度与阻断是两件事，不得混为一谈（深度可到 L4，阻断只在发布门）。 | implemented（ADR-0010 发布门 + release.yml release-gate；real-site-smoke 无 pull_request 触发面） |
| **D-006** | Q4 — 验收面的最终清单与切法：(a) 18 项是否即全集？(b) 持久化（reload 复验）是否定为通用判据？(c) 跨隔离上下文链路是否单列？ | `走你推荐的吧` | **（a）接受 18 项但减 1**：`__cchLastFill` 三态钩子按 D-002 降为**诊断项、移出验收面** → **17 项验收 + 1 项诊断**；**（b）持久化（reload / 重开面板后效果仍在）定为每项有状态工具的通用判据**（收藏、语言、低调样式、豁免、规则）；**（c）跨隔离上下文链路单列为独立验收项**，且**必须断言链路两端的写入结果**（子帧图标点击 → `postMessage` → 顶层代开面板；子帧选国 → `postMessage` → 子帧 `Fill.run`）。 | **`__cchLastFill` 不得再充当验收断言依据**（现 `verify-ticket-31.mjs` 以它为据，属待回写的既有实践）；持久化复验不得只读 storage 自证（须经用户可见行为复验，D-002）；跨上下文验收不得只断言「消息到了」而不验两端结果；不得因减 1 而降低其余 17 项的断言强度。 | implemented（17 项 + 1 诊断项 / 持久化通用判据 / 跨隔离上下文 #14 双端断言，见 ACCEPTANCE-SURFACE.md） |
| **D-007** | 域建模待办：是否为本轮新增机制立 ADR / 补 CONTEXT.md 术语？ | `OK`（在「走你推荐的吧」之后，认可方向） | **为「发布门」立一条 ADR**（PR 不阻断、发布门阻断的取舍需向未来读者解释）；**CONTEXT.md 补两个术语**：「验收阶梯」（L0–L4 五级定义与层归属）与「发布门」（真实站点层绿或显式 ack+立票方可出包）。**执行时机：定稿前呈报执行**。 | 术语入库前不得与既有术语冲突（CONTEXT.md 现有 29 条）；ADR 不得改写 ADR-0008 第二层（D-005 已定不重开）；不得为可逆/无取舍的小事立 ADR（域建模纪律）。 | revised → implemented（ADR-0010 成文；术语条数条款由 D-013 覆盖为 7 条） |
| **D-008** | Q5 — owned 指定页面语料以什么形态进仓库？**（调研结论与 ADR-0006 条款 5 冲突，已触发禁静默改向）** | （调研结论，非用户原话）用户指令：`将当前问题5提交 atomcode-research 深度调研……调研的结果辩证性看待` | **采纳调研的「三层架构」**：① **本地镜像页**（入 git，测试主力；去品牌/去追踪/内容替换，断言确定性最高）；② **结构骨架**（入 git，长期结构断言基线）；③ **原始快照**（**库外**档案：SingleFile HTML + WARC + 截图），**库内只留 SHA256 指纹清单 + 元数据 JSON**（`source_url` / `captured_at` / `sha256_html` / `mirror_of` / `license_note`）；**目录适配到 ADR-0006 条款 5 的单一 `tests/` 根**；库外档案**复用已有的仓库外 archive 模式**（不引入 S3/对象存储）；改版检测走四步回路（定期重捕 → **确定性结构 diff（非像素 diff）** → 分级 bump 版本 v3→v4 → **回放自检**）；纪律「**绝不为修绿而盲目更新快照**」。 | **① 不得在仓库根新增顶层 `corpus/` 目录**（调研模板提的顶层布局与 ADR-0006 条款 5「仓库只有一个 `tests/` 根」直接冲突）——除非用户明确要求重开该 ADR；**② 不得引入对象存储/S3 依赖**（调研建议的 S3/B2 在本仓库无基础设施，复用既有仓库外 archive）；**③ 原始快照（含品牌内容）不得入库**（ADR-0006「冻结基准不入库」+ 版权口径）；**④ 截图/二进制不得进 git 历史**；**⑤ 新语料不得与既有「校准语料」混淆**（`tests/corpus/manifest.json` 已存在，41,887B / 51 例，是 precision/recall 回归基线；两者术语与路径必须区分）；**⑥ 不得为修绿而盲目更新快照**。 | implemented（票 06 三层架构：镜像 9 + 骨架 8 + manifest 指纹 + 仓外 archive；单一 tests/ 根；无 S3） |
| **D-009** | 域建模：新语料叫什么（避开与「校准语料」混淆）？CONTEXT.md 最终术语清单？ | `接受`（承接上一轮三件拍板，含命名选项 (i)） | **命名：采用「形态语料」**，路径 `tests/corpus/forms/`；与既有「校准语料」在语义轴上正交（一个管**表单形态**、一个管**阈值标定**）。**CONTEXT.md 术语清单最终定为 5 条**：「验收阶梯」（L0–L4 五级定义与层归属）·「发布门」（真实站点层绿或显式 ack+立票方可出包）·「形态语料」·「结构骨架」·「镜像页」。**「SHA256 指纹清单」与「语料退化回路」不单列术语**（属实现机制而非领域概念，按 domain-modeling 纪律 CONTEXT.md 应无实现细节）。 | **不得与既有 `tests/corpus/manifest.json`（校准语料，41,887B / 51 例）撞名或混淆；不得为同一概念造两个术语；术语入库前必须与既有 29 条逐条比冲突；不得把实现细节（指纹/回路步骤）写进 CONTEXT.md。** | revised → implemented（命名「形态语料」落 tests/corpus/forms/；术语条数由 D-013 覆盖为 7） |
| **D-010** | Q6 — 设置面的形态：组织(a) + 入口(b) + 语言控件形态(c)（+ 重渲染方式(d)） | `③` | **只做 (b) + (c) 第二形态**：**(b) GM 菜单新增「设置」项**（补上零置信度入口）；**(c) 语言控件改为三选一显式控件**（`自动（跟随浏览器）` / `中文` / `English` 并列可见）。**(d) 默认采纳**（用户未否决）：切换语言后改为**字典化全量重渲染**（`[data-i18n]`）替代手工逐项刷新。**(a) 明确不采纳**：**不新建独立「设置」视图、语言控件不前置**。 | **不得新建独立「设置」视图；不得把语言控件前置于视图首部（(a) 被否）。** GM 菜单「设置」项必须打开面板**并显式切到设置所在视图**（面板默认是列表视图 `ui:296`，不能只开面板）；不得因 (c) 而改变 `LOCALE_MODES` 取值语义（`auto/zh/en`）与持久化键（`UI_PREFS_KEY`）；(d) 不得改变 `t()` 契约与既有中/英文案键。 | implemented（票 02：(b) GM 设置项 + (c) 三选一控件 + (d) 字典化重渲染；(a) 未采纳——未新建视图、未前置） |
| **D-011** | Q7 — ③ 的残余后果（语言控件位置随豁免域名数量浮动）怎么处理？(A) 接受残余 / (B) 入口负责滚动定位（零结构改动）/ (C) 允许最小上移 | （调研结论，非用户原话）用户指令：`将当前问题提交 atomcode-research 深度调研……调研的结果辩证性看待` | **调研支持 (B)**：成熟产品**事实标准是由入口负责把目标设置项滚动到可见区并高亮**，而非让用户自己滚动查找（Warp `?widget=` · macOS `x-apple.systempreferences:<pane>?<anchor>` · VS Code 命令面板 · canopy `scrollToSectionId`）；且**深链定位优于重排顺序**（重排破坏肌肉记忆与既有外部引用，且只解决「浏览」不解决「直达」；两者不互斥，但深链才是默认做法）。**可复用模板**：入口契约用**稳定 slug 白名单**；路由优先级 `target(精确) > q(过滤) > section > 默认页`；打开策略「未开→打开并路由；**已开→原地复用并应用新参数，不重复入栈、不清返回栈**」；定位时机**等目标 DOM 真实存在再滚**（Suspense commit 后同步滚 或 MutationObserver 静默 500ms 阈值）；`scrollIntoView({block:center})` + **高亮脉冲 ~1.5s 后衰减**；滚动后 `focus()` 到目标（`tabindex=-1`）；回归测试「同一深链连按两次不入栈两次」（Firefox iOS #34090 教训）。 | **不得重排设置顺序**（调研与 D-010 一致：重排破坏肌肉记忆与外部引用，且不解决「直达」）；**不得用固定延时猜测滚动时机**（DOM 异步渲染下原生锚点会失效）；深链目标**不得绑定内部实现名或易变排序位置**（须稳定 slug 白名单）；高亮**必须有衰减**（不得持续抢注意力）；**不得因定位而改变 D-010 (a) 被否的前提**（不新建视图）。 | implemented（票 02：稳定 slug 深链 + 滚入可见区 + 高亮衰减 + 已开面板原地复用） |
| **D-012** | Q8 — 诊断面（G2「可解释」）的形态与深度：(a) 数据源与出口 (b) 覆盖面 (c) 承载与触发 (d) 开销约束 | （调研结论，非用户原话）用户指令：`将当前问题提交 atomcode-research 深度调研……调研的结果辩证性看待` | **（a）共享同一份结构化诊断事件流**——面板与机器可读输出是同一数据源的两个 **serializer**（渲染层分离 ≠ 采集层分离）；**（b）覆盖到「写入结果」全链路**：三层判定（工具失效 / 注入失效 / 脚本逻辑失效）+ 第四层写入结果（提交前状态 → 写入动作 → 提交后断言 三元组）；**（c）独立诊断视图承载重诊断**（决策链时间线 + 过滤器 + 导出）**+ 既有界面做入口与摘要**（总健康状态点 + 当前页各工具生效与否一行摘要 + 「打开完整诊断」按钮）；油猴约束下入口收敛为**一个** GM 菜单项打开面板；**（d）分级门控**——恒开 = error/warn + 计数器；门控开 = 全链路 decision trace / dump；惰性构造（`log.debug(() => buildDump())`）；环形缓冲固定容量（每页 ~200 条）。**附**：`doctor` 作为**第二机器面**，与面板**同享检查矩阵定义**（id/status/message/fix），仅 runner 不同。 | **不得各自实现采集逻辑**（面板与 JSON 双写必然漂移，「面板显示 ok 但 JSON 报 fail」本身就是需要诊断的诊断 bug；唯一可接受的分离是**渲染层**）；**不得把 error/warn 与计数器也门控掉**（「默认零开销」指**高开销诊断**，不是零可观测——这是对本大脑代理 (d) 提案的修正）；**任何 fail 记录的 reason 必须指向已验证因果**（TM 误导横幅是反例）；**不得为每个诊断功能各设一个 GM 菜单项**（TM #2461 无子菜单/无图标）；环形缓冲必须有容量上限；诊断不得挂在运行热路径上拖慢扫描（1000 节点 < 350ms 红线）。 | implemented（票 03：单采集源双 serializer + 四层判定 + 独立诊断视图 + 分级门控 + 环形缓冲） |
| **D-013** | 术语清单：账本（D-007 / D-009）写 5 条，对话里说过 7 条——裁定？ | `a`（在「对上面 4 项各选一个处置」的 (a)/(b)/(c) 中选 **(a) 落账**） | CONTEXT.md 术语清单由 **5 条修订为 7 条**：在「验收阶梯 · 发布门 · 形态语料 · 结构骨架 · 镜像页」之上新增 **「诊断面」**（D-012 的独立诊断视图 + 既有界面入口摘要）与 **「判定记录」**（D-012 (b) 的四层判定记录）。D-007 / D-009 的条数条款作废，其余保留。 | 术语不得与既有 29 条冲突；**不得把实现细节（SHA256 指纹清单 / 语料退化回路步骤）写进 CONTEXT.md**；不得为同一概念造两个术语；**7 条为上限，不得继续膨胀**。 | implemented（CONTEXT.md 7 术语落盘，28→35；零实现细节） |
| **D-014** | 页面候选清单（8 个指定页 + 形态分类 + 覆盖缺口）是否落账？ | `a` | 将 Cycle-6 页面侦察产出的 **8 个最佳指定页候选**（`react-phone-number-input` 演示页 / `intl-tel-input.com` v29 / `cdpn.io` ExzVrPY v17 / `mui.com` / `element-plus.org` / `ant.design` / `harvesthq.github.io/chosen` / `signup.heroku.com`）及其**形态分类**与**覆盖缺口**登记为**输入材料**，供 M5（形态语料）取用。授权依据：D-001 / D-008 的「页面选取与维护权委托 Agent」。 | 候选页为**候选非承诺**——入库前必须过 D-008 的合规口径（只采公开页 / 剥离品牌内容 / 标 `source_url` + `captured_at`）；**不得把原始快照入库**；**不得把候选清单当成语料清单**（语料以镜像页 + 结构骨架为准）。 | implemented（票 06 取用 8 候选页为输入材料；原始快照出仓不入 git） |
| **D-015** | harness 现状（密封层无交互原语层）是否落账？ | `a` | 登记事实：`tests/helpers/userscript.ts` 仅 **48 行 / 4 个导出**（`userscriptCode` / `installUserscript` / `wrapperFor` / `openPanel`），**密封层本身没有可复用的交互原语层**——每个 spec 各自写点击。因此 M4 的「建交互原语」**不是补 live 层，而是从 L1 起就缺失**。 | 该事实**不得被解读为「只需给 live 层加原语」**；M4 必须从密封层起建，且两个 harness **收敛为同一份原语**（不新造第二套）。 | implemented（票 05：从密封层起建 primitives，两 harness 收敛同一份，不新造第二套） |
| **D-016** | 三个新发现的误报面 + 一处机理不符是否落账？ | `a` | 登记四项调研发现：① **iti v29 内部搜索框** `INPUT[role=combobox][type=search][aria-autocomplete=list]` 就在 `.iti` 容器内——真实误报面；② **值恰为 ISO2 的语言下拉**（伪区号陷阱）；③ **无括号区号文本**（`AC|Ascension Island +247`）——现有 `parenDial` 只认括号，L3 漏此形态；④ **视觉替换型隐藏 select 的机理与语料假设不符**：Select2 实测 `width:1px` + `aria-hidden`（**非零尺寸**，`_hiddenByStyle` 不触发），与语料 N7 假设的隐藏机理不同；且 `tests/fixtures/` 内 `select2|chosen` **零命中**。 | 这四项为**阶段 B 输入**（按 D-001 的「先建验证能力、再由失效清单驱动修复」），**本周期不预先立独立修复票**；**不得据此直接改检测代码**（沿用 A-023 的「无地基不立检测改动」约束）。 | implemented（票 08：四项发现落语料/据实修正；阶段 B 由验证清单驱动，未预先改检测） |

## 覆盖率自评

- 已确认条目数：**13 current**（D-001 / D-002 / D-004…D-008 / D-010…D-016）| revised：3（D-003 / D-007 / D-009）| 待拍板：**0**
- 本周期摩擦点（A-026…A-030）覆盖：**4/5**（含 D-008/D-009 对 A-030 语料形态的落地）（D-001→A-029/A-030 目标形态与页面授权；D-002→A-028 判定依据；D-004→A-029/A-030 阶梯深度与层归属；D-005→A-029 真实站点层触发面与失败语义；D-006→A-029/A-030 验收面清单）| **A-026 / A-027（设置面与 i18n）未动**
- 待决问题（frontier）：Q9 = 台账两条机械性条款是否原样保留（A-027 菜单 {id} 原地更新；A-029 harness 交互原语 + soft-assert）；裁决后覆盖 5/5 → 进入定稿问询


## Cycle-6 结算（2026-09-17，收口票 09）

> 结算依据：逐票实物核验（issue 勾销 + 窗口报告 + 实施提交 sha + CI run）；行为面只认 CI run（WORKFLOW §8.1）。
> 结算结果：D-001…D-016 = **16/16 落定**（13 current → implemented；3 revised → implemented，修订后的口径生效）。
> 覆盖核对：本账本 16 条全部有票去向（D-001/D-002/D-003/D-004/D-006/D-014/D-015/D-016 → 票 01-08；D-005/D-007/D-013 → 票 04；D-008/D-009 → 票 06；D-010/D-011 → 票 02；D-012 → 票 03）；无去向记录 **0 条**。
> 与 A 台账的对应：D-004 恢复 A-029/A-030 原口径；D-010 修订 A-026；D-013 修订 D-007/D-009 术语条数。
> 遗留：无未决条款；后续周期若重开「L4 不自动化」或「真实站点止于 L0-L1」，须先重开 D-004 与 ADR-0010。
