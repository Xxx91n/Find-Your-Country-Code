# Cycle-9 实施报告 —— 发版收割 v1.8.0 ＋ 收口债清账

> **时点标注（硬要求 · D-005② / FR-12 教训）**：本报告内所有 run ID、sha、Release 链接、GF 同步状态均为 **2026-09-18 的时点快照**，**不得当作可复跑的期望值**。凡「可复跑」项均附命令。
> 生成：2026-09-18｜W2 分支 `cch/18-cycle9-grill` · W3 分支 `cch/19-cycle9-closeout`｜基线 `origin/main` = `752c4bb2` → 终态 `28dda083`
> 任务书：`.scratch/cycle9-grill/handoffs/next-round.md`｜账本：`.scratch/cycle9-grill/decision-ledger.md`（D-001…D-005 全 current）

## 0. 一句话结论

**A（发版收割）＋ C（收口债清账）两条线均已落地**：`v1.8.0` 已发布（tag ＋ Release ＋ `.user.js` 附件）；发布门以 **T-01 补跑的绿 run** 为依据（**未使用 ack**）；C 侧 register / ADR / `CONTRIBUTING.md` / 备份 ref 全部清账。硬验收本地与 CI **全绿**。

## 1. 波次执行摘要

| 波次 | 票 | 结果 | 落地 sha |
|---|---|---|---|
| W1 | T-01 | ✅ 补跑绿（run `35354287491`，gate=pass，26 阶梯断言 / 0 失败） | — |
| W1 | T-02 | ⏭️ **未触发**（条件票：仅补跑红时发生） | — |
| W2 | T-03 | ✅ bump 1.8.0 ＋ lockfile 对齐 ＋ Glog 双语 5 条 | `8032490e` |
| W2 | T-04 | ✅ push ＋ land → Release `v1.8.0` | `8032490e` |
| W3 | T-05 | ✅ 追溯面入 register §10.1 → 删 `refs/backup/main-pre-rewrite` | `28dda083` |
| W3 | T-06 | ✅ register 回写 ＋ ADR 4 篇状态行 ＋ `CONTRIBUTING.md` 同步 | `28dda083` |
| W3 | T-07 | ✅ 清账 land（**未触发 release**） | `28dda083` |
| W4 | T-08 | ✅ 本报告 | — |

## 2. 逐任务证据（每条附可复跑命令 ＋ 输出摘要）

### T-01 补跑 `real-site-smoke`（D-002③ · D-004①）

```
gh workflow run real-site-smoke.yml --ref main
gh run view 35354287491 --json status,conclusion,headSha
```

- run ID **`35354287491`**｜conclusion **`success`**｜headSha **`752c4bb2…`**｜event `workflow_dispatch`｜branch `main`
- **非空绿核验**（避「只引入却没做到」）：下载 artifact `live-out/smoke-summary.json` ⇒ `gate: "pass"`、`counts: {selected:8, runnable:6, skipped:2, observed:3, ladderChecks:26, ladderFailures:0}`、`violations: []`、`failures: []`、`launch: "headed"`；6 个目标跑满 L0–L4；2 个 skip 均带 `ticket=32` ＋ reason；`白名单契约 ＋ 全阶梯契约 ＋ harness 自证: PASS`
- ⚠️ **sha 差异（D-004③）**：补跑 sha = `752c4bb2`，**发版 sha = `8032490e`** ⇒ **不得声称「补跑验证了被发版的代码」**。ADR-0010 发布门判据本身为「默认分支上**最近一次运行**为绿」，本窗口补跑即满足。

### T-02（条件票）—— 未触发

补跑为**绿** ⇒ 按 D-002③「若补跑为红 ⇒ 走显式 ack」的反面，**不进入 ack 路径**。`.github/release-gate-ack.json` 保持 `acknowledged: false`（**未改动**，`git diff` 对该文件为空）。

### T-03 bump ＋ lockfile ＋ Glog

```
npm version 1.8.0 --no-git-tag-version
```

- `package.json`：`1.7.0` → **`1.8.0`**（唯一真源；`vite.config.ts` 构建时注入）
- `package-lock.json`：根 `version` `1.5.0` → **`1.8.0`**（2 处：顶层 ＋ `packages[""]`）—— `git diff --stat` = `3 insertions / 3 deletions`（**仅版本字段**）
- `greasyfork/Glog.md` ＋ `Glog_EN.md`：新建 `## v1.8.0 更新日志` / `## v1.8.0 Changelog`，**中英各 5 条、逐条对应**（含 D-005① 两处措辞：「低置信档，仅低调提示」/「密码管理器生态的通用退避约定」）
- 事实核对（**逐条对 `v1.7.0..HEAD` diff 复核**，非按提交信息归类）：① `contenteditable` 三重口径选择器入候选 ② `:not([contenteditable="false"])` 排除 ③ `isOptOutElement()`（`data-1p-ignore` / `data-form-type="other"`）短路 ⇒ `gate:optout` / `score:0` / `tier:none` ④ `['+383','XK',…]` / `['+379','VA',…]` ⑤ `if (traceFlag) console.warn('[cch][diag] …')`

### T-04 提交 → push → land → 验 gate ＋ Release

```
but commit -b cch/18-cycle9-grill -m "chore(cch-18): 发版收割 v1.8.0 …" kt pz kp sz
but push cch/18-cycle9-grill
but land cch/18-cycle9-grill --yes
```

- 分支 CI（ADR-0014 决策 1 增量确认，sha `8032490e`）：`Verify Tickets` **21/21**、`E2E` **151 passed**、`Typecheck` / `Engine Gates` / `Lockfile Regen` 全 success
- 落地后 `main`（`8032490e`）：**7 run 全 success**（含 `Auto Release on Script Update`、`Verify Tickets` 21/21、`E2E` 151、`Calibration Baseline`）
- **发布门**（release run `35355298758`，job `release-gate` = success）日志原文：`结论：**通过（绿）**` ＋ `真实站点层最近一次运行: id=35354287491 conclusion=success`
- **Release**：`v1.8.0`（tag → `8032490e`）｜附件 `find-your-country-code.user.js` **171,172 B**｜URL：`https://github.com/Xxx91n/Find-Your-Country-Code/releases/tag/v1.8.0`

### T-05 追溯面 → 删 ref

```
git rev-parse b1fcf96d^{tree} d075f01a^{tree}     # 均为 a4c9f253…
git update-ref -d refs/backup/main-pre-rewrite
git for-each-ref refs/backup | wc -l               # 0
```

- 追溯面已入 register **§10.1**（3 旧 sha ＋ 3 新 sha ＋ 逐提交 tree ＋ 重写时点口径澄清）
- **先写后删**：register 写入并**提交**（`xlr`）后才删 ref
- 删除后 `refs/backup` 计数 = **0**；远端本就无该 ref
- ⚠️ **不得声称旧 sha 仍可恢复**

### T-06 C 侧清账

- register **§10.2**：9 条待签核**逐条列明**（用户签核 2026-09-18）＋ FR-04/05/06/15 转 closed；§三追加 **FC-10…FC-15**
- register **§10.3**：ADR `0007`/`0008`/`0010`/`0011` 状态行**规范化**（取值逐字不变，原文保留于 §10.3）
- `CONTRIBUTING.md`：新增 `## 发版节奏` → **ADR-0016**；`## 发布链路（自动化）` 补「落地口径」→ **ADR-0014**

### T-07 清账 land（不触发 release）

```
but push cch/19-cycle9-closeout
git diff --name-only origin/main origin/cch/19-cycle9-closeout | grep -E '^(src/|vite\.config\.ts$|package\.json$|package-lock\.json$)'   # 零命中
but land cch/19-cycle9-closeout --yes
```

- 变更面 = **6 文件**（register ＋ `CONTRIBUTING.md` ＋ 4 ADR）—— **不含任何发布触发路径** ⇒ `NO_RELEASE_TRIGGER_PATH`
- 落地后 `main` = **`28dda083`**；**6 run 全 success**
- **未触发 release**：`gh run list --workflow=release.yml` 最新仍为 `35355298758`（`8032490e`），**无新 run**

## 3. 硬验收总表（D-004④）

| 面 | 项 | 结果 | 证据 |
|---|---|---|---|
| 本地 | typecheck | **0** | `npm run typecheck` exit 0 |
| 本地 | build 可复现 | **✅** | `a920813a1dd75e26a066add009622662adc2cd0cbc1f94807150f5c7bba9a4cb` ×2 次一致；171,172 B |
| 本地 | E2E | **151 passed** | `npx playwright test` |
| 本地 | 21 票级门 | **21/21** | `verify-ticket-runner.mjs --run <k>` × 21，`pass=21 fail=0` |
| 本地 | `--audit` | **19-0** | `AUDIT 19 passed / 0 failed` |
| 本地 | calibration | **P=R=F1=1.0** | `cases=63 P=1 R=1 F1=1 gate=pass` |
| 本地 | real-site gate | **pass** | run `35354287491` |
| 本地 | `git diff --check` | **净** | exit 0 |
| CI | land 后 main | **全 success** | `8032490e` 7 run；`28dda083` 6 run |
| CI | `Verify Tickets` | **21/21** | run `35355298958` |
| 发布门 | 绿或 ack | **绿** | run `35355298758`，`结论：通过（绿）` |
| Release | tag ＋ Release ＋ 附件 | **✅** | `v1.8.0` → `8032490e`；附件 171,172 B |
| C 侧 | register 终态 | **✅** | 15 条 FR 全有终态且全部具名签核（§10.2） |
| C 侧 | ADR 状态行 | **✅** | 16 篇全有（4 篇已规范化） |
| C 侧 | CONTRIBUTING | **✅** | 指向 ADR-0014 / ADR-0016 |
| C 侧 | 备份 ref | **✅** | 已删 ＋ 追溯面已写入（§10.1） |

## 4. 账本结算（D-001…D-005 → 去向）

| D | 去向 | 落实证据 |
|---|---|---|
| **D-001** | A ＋ C 两条线；排除 B/D | A：Release `v1.8.0`；C：§10.1–§10.4；未做 FR-11 / B 侧扩张 |
| **D-002** | ① v1.8.0 ② 66 提交一次收割 ③ 先补跑 ④ Glog 双语 ⑤ A 先 C 后 | ① `package.json` 1.8.0 ② `git rev-list --count v1.7.0..752c4bb2` = **66**；`git diff --shortstat v1.7.0 752c4bb2` = 7674+807 = **8481 行** ③ run `35354287491` ④ Glog 中英各 5 条 ⑤ W2 先于 W3 |
| **D-003** | ① register 回写 ② 9 条签核 ③ ADR 形式债 ④ CONTRIBUTING ⑤ ref 删除 ⑥ lockfile 对齐 | ① §10.2.2 ② §10.2.1 ③ §10.3 ④ §10.4 ⑤ §10.1 ⑥ lock 根 version = 1.8.0 |
| **D-004** | ① 授权 1/3/4/5/6/7 ② ack 条件触发 ③ 顺序 ④ 硬验收 ⑤ 诚实边界 | ① 见 §2 ② 未触发（绿）③ W1→W4 ④ §3 ⑤ §5 |
| **D-005** | ① Glog 定稿 ② 收口形态 (A)+(B) | ① Glog 双语 5 条 ② 账本结算（本节）＋ 本报告 ＋ 再生 handoff |

**无去向记录清单：空（5/5 均有去向）** ⇒ 结算通过。

## 5. 诚实边界与残留（D-004③④⑤ · D-005②③④）

- **GF 同步状态 = 待验（≤ 1 天）**：GreasyFork 为拉取式镜像（Sync ≤ 1 次/天），**不得声称已送达 v1.8.0**。`greasyfork/GREADME*.md` 内「观测于 2026-09-17 / 1.7.0」段落为**当日观测**，本窗口**未改写**（改写需重新观测，且不得据此声称同步完成）——**登记为残留**。
- **补跑 sha ≠ 发版 sha**（`752c4bb2` ≠ `8032490e`）。
- **FR-01…FR-03 · FR-09…FR-14（10 条）** 到期 **2027-03-31**，**绝不自动续期**。
- **FR-11 门槛②** 仍不可判定（语料未建）；本轮未声称达成或失败。
- **`test-results/锐.txt`** 输入缺口**第二次连续缺失**，未做锐评核验。
- **T-09（下一轮 frontier）未拍板** —— 账本无对应 D-xxx，**未抢跑**。
- **FC-09 未入 §三 索引**（登记在 §九，Cycle-8 遗留）；本窗口**未改动**，登记为观察项。

## 6. 偏离与更正登记（本窗口）

1. **ADR 形式债口径更正**：账本记「4 篇缺 MADR 状态行」；实测 16 篇**均有**状态行，4 篇的实际债是**字段顺序 ＋ 分隔符偏离约定**。已按约定规范化（取值逐字不变），原文保留于 register §10.3。**未新增 ADR / CONTEXT 术语**。
2. **任务书 T-05 措辞「旧 tip tree == 当前 main tree」**：该等式成立于**重写时点**（`b1fcf96d^{tree}` == `d075f01a^{tree}` == `a4c9f253…`）；本窗口**当前 main = `8032490e`**（tree `a9f790cb…`）已前进 ⇒ 已在 register §10.1 明确**不得延伸解读**。
3. **`git update-ref -d` 为 `but` 之外的唯一路径**：删除任意 ref 无 `but` 等价命令，按 **D-004① 第 6 项**显式授权执行；**未使用任何其他裸 `git` 写操作**（无 `git commit`/`push`/`reset` 等）。

## 7. 复跑清单（命令可复跑；数字为 2026-09-18 时点快照）

```
npm run typecheck                                    # 期望 exit 0
npm run build && sha256sum dist/find-your-country-code.user.js
node tests/scripts/verify-ticket-runner.mjs --audit   # 期望 19 passed / 0 failed
node tests/scripts/14-calibration-harness.mjs         # 期望 P=R=F1=1
npx playwright test                                   # 期望 151 passed
git for-each-ref refs/backup | wc -l                  # 期望 0
grep -c '^状态：' docs/adr/*.md                        # 期望每篇 1
```
