# 13 — 可见性闸门与 L3 内容验证加码

**What to build:** 隐藏/不可达字段不再注入图标(降为登记+手动召唤),同时 L3 选项验证升级(ISO2 值域成员测试、共享区号消歧、占位首项剔除),误报再收敛且不漏杀合法字段。

**Blocked by:** 16 — 检测评分一致性收尾

**Status:** ready-for-agent

- [ ] 可见性闸门作用于注入档位: display:none/零尺寸/opacity:0/clip-path/content-visibility/遮挡判定的字段不注入,保留检测登记与手动召唤路径
- [ ] 隐藏但承载值的原生 select(视觉替换型自定义下拉)仍可经面板填充,不被闸门误杀
- [ ] L3 ISO2 判定改为对数据全集成员测试,替换「像 2 字母」形态学猜测
- [ ] 共享区号(+1/+44 等多国)选项消歧走文本+国家名,填充结果正确
- [ ] 占位首项(请选择类)不参与内容验证计分
- [ ] 既有误报样本全不注入 + 正样本不回归,证据走 CI
