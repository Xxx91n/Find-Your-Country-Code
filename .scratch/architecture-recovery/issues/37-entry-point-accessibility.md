# 37: 入口可达性

**What to build:** 让任何页面（含低置信页面）都能打开面板并召唤已登记字段；让 lowkey 图标在真实表单里可见且不被裁剪。

**Blocked by:** None（可立即开工）

**Status:** ready-for-agent

**覆盖 A-xxx:** A-012, A-013

- [ ] 新增 GM 菜单“打开面板”命令，点击后面板在无任何图标的页面上正常渲染（居中）
- [ ] 低置信页面（best score < 35 但 ≥25）能通过该入口召唤已登记字段并补挂图标
- [ ] lowkey 图标在典型 `overflow:hidden` 祖先容器下不被裁剪
- [ ] lowkey 与高置信图标的视觉分层语义保留；悬停恢复行为保留
- [ ] 新增密封断言覆盖上述两条路径
