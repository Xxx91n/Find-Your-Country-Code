intl-tel-input（一个被广泛使用的国际电话号码输入库，历经 v16 到 v29 多次重构，API 名称与 DOM 类名都改过）在被外部代码通过它的官方实例 API 编程式地切换国家、或写入号码（而不是让用户手动去点它的国家下拉）之后，官方语义到底保证发生什么？

请回答四个问题：

1. 该库是否承诺把「所选国家的国际区号」写进它接管的那个 input 元素的 value？注意区分「只切换国家」与「写入一个号码」两种动作，以及写入的号码里带不带国际区号。

2. 编程式切换国家之后，该库会在页面上留下哪些「能被外部观察者读到」的国家状态？这些状态里哪些是官方文档承诺的、哪些只是内部实现细节？它们在 v16 到 v29 的重构中是否稳定？

3. 这些 API 调用会不会派发标准 DOM 事件（input / change / blur 等）？官方文档或源码怎么说？

4. 假设有一个自动化测试框架，它只允许用「浏览器侧外部可观测事实」（DOM 状态、字段 value、DOM 事件）来断言，并且明令禁止读取被测脚本的内部状态与自报结果。那么对这个库所管理的字段，「已被成功设置为指定国家」这件事的正确可观测判据应该是什么？业界做自动化测试时实际怎么断言的？

请给出官方文档与源码依据、版本差异，并给出可核验来源。