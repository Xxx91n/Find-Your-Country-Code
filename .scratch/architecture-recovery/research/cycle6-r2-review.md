# Cycle-6 R2 轮次复核（票 02 去污染移交包）— 首脑，2026-09-16

> 复核人：大脑 Agent | 方法：**不信自述（也不信自己的自述），逐条回仓库实物 + 隔离环境实跑**
> 对象：`research/cch02-r2-README.md` + `cch02-r2-decontamination.patch` + `cch02-r2/src/{ui/index.ts,i18n.ts}` + 报告 `## 返工轮次 R2` 节
> 结论：**R2 交付全部属实、可复现；落地未完成（移交大脑）；本人 §7 有三处事实错误已自纠**

## 一、结论摘要

| 维度 | 判定 |
|---|---|
| 修复正确性（是否真去污染） | ✅ 票 03 残留 **0**（`票 03`/`DIAG_`/`diag`/`PanelView` 全 0），票 02 自有 delta **11 处标记全保留** |
| 修复充分性（是否自洽可构建） | ✅ 隔离环境实跑：`build` exit 0 · `typecheck` exit 0/0 错 · 门 02 33 PASS/0 FAIL · 门 42 42 PASS/0 FAIL · 全量 E2E **103 passed / 1 failed** |
| 产物完整性 | ✅ 尺寸/行数/SHA-256 与自述**逐字命中**；补丁 `-p1` 干净可施且结果**逐字节等于**两个全文 |
| 落地 | ❌ **未落到分支**（cch/02 仍为 b95f672f / 本地 32dc7bd0，仍不可构建） |
| 我方 §7 事实 | ⚠️ **三处错误**（见 §六），窗口的反证**全部成立** |

## 二、产物完整性（实物）

| 文件 | 行数 | 字节 | SHA-256（实测） | 与自述 |
|---|---|---|---|---|
| `cch02-r2/src/ui/index.ts` | 861（含末行=862） | 43,319 | `864fda62bff01eea…` | ✅ 逐字命中 |
| `cch02-r2/src/i18n.ts` | 54（含末行=55） | 6,110 | `1ce49046b4837c83…` | ✅ 逐字命中 |
| `cch02-r2-decontamination.patch` | — | 23,634 | `b56939ff03f25d93…` | ✅ 逐字命中 |

补丁**纯删除式**：`ui/index.ts` 各 hunk 删除数合计 2+2+32+21+7+215+3+16 = **298**（1160→862，数学自洽）；`i18n.ts` −2/−2 = **−4**（59→55，删的正是票 03 的 diag 串块 zh/en 各 2 行）。

## 三、补丁可施性与字节等价（**施于当前 cch/02 内容，非仅孤儿 sha**）

- 隔离检出：`git archive cch/02-settings-surface` → 411 文件。
- `patch -p1 --dry-run` → **checking file … ×2，无 fuzz / 无 reject**；正式应用同样干净。
- 应用后 `sha256sum` = `864fda62…` / `1ce49046…`，`diff -q` 对两个全文**逐字节相等** ⇒ README 的等价性声明**成立**。
- 被删 i18n 键（`diag*` / `diagnostics`，共 26 键 ×zh/en）在 `src/` 内**零悬挂引用**。

## 四、硬验收实跑（红 → 绿，主 Agent 独立复现）

| 门 | 我的实跑 | 结果 |
|---|---|---|
| 红（修复前） | `vite build`（隔离树，未施补丁） | **exit 1** —— `src/ui/index.ts (4:41): "DIAG_TRACE_PREF" is not exported by "src/config.ts"`，与 CI `35089321289` **逐字一致**（仅耗时 329ms vs 148ms） |
| 绿（修复后） | `vite build` | **exit 0** —— `dist/find-your-country-code.user.js 132.73 kB`（1.65s） |
| 绿（修复后） | `tsc --noEmit` | **exit 0 / 输出 0 行** |
| 结构门 | `verify-ticket-02-settings.mjs` | **33 PASS / 0 FAIL**（exit 0） |
| 回归门 | `verify-ticket-42.mjs` | **42 PASS / 0 FAIL**（exit 0） |
| 全量 E2E | `playwright test` | **103 passed / 1 failed**（104 例，54.5s） |

**唯一红** = `tests/entry-access.spec.ts:40`（断言 `:53` 垂直居中 `toBeLessThan(30)`，实测 **33.12236022949219**）= **R-3**，与本票无关。

## 五、静态正确性（逐项实物）

| 声明 | 实物 | 判定 |
|---|---|---|
| 类型 import 行还原为基线 12 项 | 基线 `50383e3c:src/ui/index.ts:4` 与修复后 `:4` **完全同一行** | ✅ |
| `_view` 类型收窄替换 | `:29 _view: 'list' as 'list' \| 'rules'`（无 `PanelView`） | ✅ |
| 票 02 标记保留 11 处 | `grep -o "票 02"` = **11** | ✅ |
| 票 03 残留全清 | `票 03`=0 · `DIAG_`=0 · `diag`(不区分大小写)=0 · `PanelView`=0 | ✅ |
| 基线 `50383e3c` 干净 | blob `42265f2ba9733e06…` · 770 行 · `票 03`/`DIAG_`/`_diagLevel`=0 · `_applyLocaleText` 仍在（票 02 未施加） | ✅ |
| 污染**仅限 2 文件** | 其余 `src/` 与全部测试文件 0 命中（本人 §7 未定位 `i18n.ts`，窗口定位到了） | ✅ **比本人更精确** |
| 窗口曾临时写主工作区并还原 | `git hash-object src/ui/index.ts` = `5f66ebb0e8…` = **cch/03 分支 blob** ⇒ 无残留 | ✅ |

**剔除范围判定（我独立复核）**：票 02 改动集内的 4 个测试文件（`entry-access.spec.ts` +16 · `helpers/userscript.ts` +22 · `iframe.e2e.spec.ts` +4 · `locale-switch.spec.ts` +19）**均为票 02 自身语义适配**（GM 菜单 2→3 条 + `cch-menu-settings` 断言 + id 原地更新替身 + 三选一语言控件 + 旧循环按钮回归钉），**不是票 03 残留** ⇒ 补丁只动 2 个 src 文件的范围**正确且完整**。

## 六、窗口对本人的反证 —— 我 §7 三处事实错误（自纠）

| 我的原陈述 | 实测 | 判定 |
|---|---|---|
| §7.1「`61f3ebe7` 不属于任何分支，是 rebase 前孤儿」 | `git merge-base --is-ancestor 61f3ebe7 b95f672f` = **YES**；`git branch -a --contains 61f3ebe7` = **5 个远端分支**（01/02/03/05/06）。**孤儿是 `06273351`**（无任何 ref 包含） | ❌ **我说反了** |
| §7.2「`src/types.ts:6` 是 `import type {... CchDiag, DiagCheck …}`」 | 两提交上 `types.ts:6` 均为注释行「`// 已知 DOM 成员与动态成员…`」；types.ts `Diag` 命中 **0**。该 import 行实际在 **`ui/index.ts:6`** | ❌ **张冠李戴** |
| §7.2 / README「`票 03` 标记 **25+** 处」 | `grep -o "票 03"` = **8**（我的 25 来自**多模式 grep 的行数**，非 `票 03` 计数） | ❌ **口径错误、高估** |

**根因**：§7.1 用了 `git branch --contains`（**仅本地 ref**）——而本地六支在 P-13 里被 rebase 过，本地已不含 `61f3ebe7`，于是我把「远端提交」误判成「孤儿」。**这正是 P-13 陷阱咬到了我自己**：判断 sha 归属必须用 `git branch -a --contains` 或对远端 ref 做 merge-base。

（次要）窗口 §R2-1 行内的计数亦有偏差：`_diagLevel|_diagLayer` 实测 **10 次**（窗口记 15）、`Diag` 实测 **25 次**（窗口记 17）。方向（整层被扫入）不受影响，属证据精度瑕疵。

## 七、R-3 归因修正（**我的假设被实测推翻**）

我先前推测 R-3 由票 03 的诊断摘要条改变面板高度所致。**实测不成立**：

- 本票已剔除摘要条，`:53` 仍以 **33.12236022949219** 失败（窗口 33.12 / CI 36.5 ⇒ **随树而变**）。
- 主工作区（并集、**含**摘要条）同一 spec 本地通过。
- 我实测：`_pos()` 在基线（`:658-687`）与 cch/02 版本**逐字节相同**（票 02 未改）。

⇒ 真实机制在 `_pos()` 的 `anchor=null` 分支：用 `pop.offsetHeight` 的**调用时高度**居中，而面板随后因 `_render()` 填充行数据**继续增高**，中心随 Δh/2 下移 ⇒ **时序 / 内容敏感**判定，与摘要条无因果。**R-3 属基线代码问题，非票 03 引入。**

## 八、我自己的跑法缺陷（方法教训，如实登记）

我第一次在隔离树跑全量 E2E 得 **92 passed / 12 failed**，并据此准备判定窗口「103/1 自述不实」。**该判定是错的，错在我**：

- `tests/server.mjs` 从 `ROOT/node_modules` 提供 `/vendor/react` `/vendor/react-dom` `/vendor/vue` `/vendor/intl-tel-input`，`/gen/react19/` 取自 `tests/vendor/react19/node_modules`（**两者均被 `node_modules/` 忽略、不入 git archive**）。
- 我未按窗口做法链入依赖 ⇒ 框架夹具 404 ⇒ 页面出现 `{{ JSON.stringify(...) }}` 原始占位符（Vue 未挂载）⇒ 11 项框架/iti/场景用例连带失败。
- 补齐两个目录链接后复跑：**103 passed / 1 failed**，与窗口自述**完全一致**（唯一红数值 33.12236022949219 vs 窗口 33.12）。

**教训（写入验收纪律）**：**隔离检出必须显式链入全部依赖根**（含嵌套 vendor 安装根），否则「框架类失败」是环境伪影而非产品缺陷；在把差异判为对方失真前，先自查跑法。

## 九、落地阻断与机制选项（**需你裁定**）

阻断事实（我认可窗口的实测）：`but commit -b cch/02-settings-surface` 报 `depends on cch/03-diagnostics-surface`。依据 GitButler 技能（SKILL.md 第 157–159 行）：这是**原子性拒绝**，官方恢复路径为 `but move <branch> --above <dep>` 或 `but branch new --anchor <dep>` —— 但那会把票 02 变成**依赖票 03**，与本票「剔除票 03 整层」的目标相反，**不可用**。

当前栈结构（`but branch list --json`）：`cch/47 → cch/48 → cch/02 → cch/03 → cch/01 → cch/05 → cch/06` **同栈**；`cch/04` 独立成栈。

| 选项 | 做法 | 风险 |
|---|---|---|
| **①（我建议）** | 自顶向下卸下 cch/06·05·01·03，使 cch/02 成为栈顶（工作树=cch/47+48+02）→ 写入 2 个修复文件 → 提交至 cch/02 → 再自底向上装回四支，必要时 `but resolve` | 重装时 **cch/03 的 `ui/index.ts` hunk 可能冲突**（其提交建立在旧 cch/02 之上）；卸/装期间工作树被缩减，**影响并行 agent** |
| ② | 绕过 GitButler：在独立检出上提交并推送 cch/02 | 违反本仓 WORKFLOW §4.2 的 `but` 唯一入口；GitButler 本地状态需人工刷新 |
| ③ | 暂不落地，先推送 cch/02 的文档提交（32dc7bd0），落地另开专用窗口 | 分支保持不可构建，P-14 持续 |

**我的建议：选项 ①，但必须由你授权**（它会churn 共享工作树，且我按纪律不得擅自改动他支状态）；执行可交回专用窗口（我可出落地启动器）。

## 十、过程违规（单独呈报，**不替你追认**）

| # | 事项 | 证据 | 待裁定 |
|---|---|---|---|
| P-15 | 报告/issue/README 的 R2 记录**照抄了本人 §7 的错误 sha 归属与计数**（`票 03`×8 正确，但 `_diagLevel`×15 / `Diag*`×17 有偏） | §六 | 是否授权补正 |
| P-16 | 窗口 README 称补丁「相对 cch/02 分支内容（61f3ebe7）」——**该 sha 实为远端 tip 的 fix 提交**（正确），但未标注本地已 rebase 到 `32dc7bd0`/远端仍 `b95f672f` | `git ls-remote` vs `git rev-parse` | 是否要求标注复核对象 |
| P-2 | cch/02 仍未推送（远端 `b95f672f`）；六支工作分支本地 tip 仍 ≠ 远端 tip | `git ls-remote` | 是否授权推送 |
| P-14 | E2E 在全部已推工作分支上 failure ⇒ **无分支可合入** | `gh run list` | 先清 R-2/R-3 才可谈合并 |

**正面记录**：窗口**先复核后动手**、**逐文件定位到 2 个文件（含我漏掉的 i18n.ts）**、**反证了主 Agent 的两处事实**、**未以补 config 导出为修法**、**临时写主工作区后逐字节还原并自曝**、**被依赖机制阻断后不硬来而呈报移交** —— 六项均符合纪律，质量高于本周期平均。

## 十一、frontier（重算）

| 项 | 状态 |
|---|---|
| 票 06 R1 | ✅ 复核通过（前轮） |
| **票 02 R2 修复正确性** | ✅ **复核通过**（可复现） |
| **票 02 R2 落地** | ❌ **未完成 → 待你裁定机制（建议选项 ①）** |
| R-3 | 待裁决（归因已修正为**基线 `_pos()` 时序问题**，建议立独立修复票） |
| W4 = 票 07 | **仍不可开工** —— 需先清 R-2 落地 + R-3，且 E2E 全支红（P-14） |
