# Cycle-6 W6 首脑复核（票 09 收口 · 票 12 A-036）— 2026-09-17

> 复核人：大脑 Agent | 方法：**不信自述** —— 逐条回仓库实物验证（门实跑 / 守卫脚本 / 分支落位 / rg 代码抽查 / CI 实物查询）+ **派子代理并行取证**，关键点由我二次抽查确证

## 一、结论摘要

| 票 | 实现 | 判定 | 返工 |
|---|---|---|---|
| **09**（收口层） | 实施提交 **6 文件 / +439 −47** 逐字命中 · 账本按实物结算 · 归档 handoff 112 行 · **R-1 归因链逐环确证** | ✅ **复核通过** | **不需要** |
| **12**（A-036） | **8 文件 / +487 −22** · 裁定=**写路径**并落地 + **ADR-0011** · 替身补 `structuredClone`+`origin` 且**断言区逐字节未变** · A/B 复现 · **S4 不再是假绿** | ✅ **复核通过** | **不需要** |

**Cycle-6 实施票 12/12 全部完成并复核通过。** 剩余动作是 **land + 发版**（不可逆，须用户逐次授权），且带 3 项前置。

## 二、「声明 → 证据 → 结论」对照表

### 票 09（收口）

| 声明 | 我的实物证据 | 结论 |
|---|---|---|
| 实施提交 `5610de77` = 6 文件 / +439 −47 | `git show --stat` **逐字命中**（README.md · decision-ledger.md · handoffs/49 · issues/09 · 09-report · cycle6-grill/decision-ledger.md） | ✅ |
| A 台账 A-026…A-035 结算为 `implemented`，A-036 不代签 | 逐行读到：A-030…A-035 = `implemented（…CI 锚点…）`；A-036 = `current（未结算）— 归并行票 12（W6），本收口票不承载、不代签` | ✅ |
| D 账本 16/16 落定 | `grep -c '\| current \|' cycle6-grill/decision-ledger.md` → **0** | ✅ |
| 归档 handoff 可恢复上下文 | `handoffs/49-cycle6-closure.md` = **112 行**；§3.2 六项「fresh context 能回答什么 → 在哪一节」映射表在位 | ✅（字节数见 P-25） |
| 两份账本各新增 `## Cycle-6 结算` 节 | `grep -c` → **1 / 1** | ✅ |
| **R-1：`cch/08` tip E2E 红，归因②（非本票改动）** | 我逐环复现：`git merge-base origin/cch/08 origin/cch/10` = **`fd02901f`**（=票 07 tip，两支已分岔）· `git diff --stat origin/cch/08 cch/10 -- tests/srcdoc-origin.spec.ts` = **15 insertions / 4 deletions**（票 08 快照携带**修复前**用例）· run **35127030916** = failure，失败点 `srcdoc-origin.spec.ts:64`（**票 10 的用例**） | ✅ **归因成立** |
| §5 逐票 CI 矩阵 | 抽验：Engine Gates 近期 run 全 success（cch/10 · cch/08）；票 03 的 CI 门 `verify-03.yml` 在 CI 上通过 | ✅ |
| §9.3 本地硬验收例外四要素 | 四要素齐（不可 CI 化 / 命令原文 / 输出摘要 / 复核窗口） | ✅ |

### 票 12（A-036）

| 声明 | 我的实物证据 | 结论 |
|---|---|---|
| 实施提交 `fe0a5df3` = 8 文件 / +487 −22 | `git show --numstat` → workflow 30 · atomcode 57 · ab 93 · gates 53 · ADR 58 · store 12 · verify-05 29/22 · verify-12 155；**+487/−22** | ✅ |
| 裁定 = **写路径**强制，并据此实现 | `src/store/index.ts`：`:27-30` 有据说明（`RULES_MAX_OVERRIDES` 为文档级不变量，强制点在写路径）· **`:273` `if (r.overrides.length >= RULES_MAX_OVERRIDES) return null;`**（在 `push`/`_writeRules` 之前）· `:220` 的 `_normRulesDoc` slice **保留**为外来输入摄取修复 | ✅ |
| 裁定有据（不得只留代码注释） | **`docs/adr/0011-rule-override-cap-enforcement-point.md`**（5,496 B）已立 | ✅ **超出要求** |
| 替身补克隆 + `origin`，**断言面零改动** | `verify-ticket-05.mjs`：`:69 const DOC_ORIGIN = SELF_ORIGIN;`（**同源定义，无第二套**）· `:77 structuredClone(msg)` · `:78 fn({ data, origin: DOC_ORIGIN })`；子代理 `diff` 断言区 → **IDENTICAL** | ✅ |
| A/B 对照实验 | `.scratch/.../research/scripts/12-ab-cap-fidelity.mjs` 实存；复跑 → A 绿 / **B 红 `99/100 FAIL S4 got=513`** / C 绿 / D 绿 | ✅ |
| 门与回归 | 我实跑：`verify-05` **100/100** · `05-harness` 59/0 · `verify-12` 31/0 · `verify-10` 34/0 · `verify-11` 62/0 · `verify-08` 35/0 · `verify-06` 209/0 · `verify-07` 63/0 · `verify-39` 27/1(G4e)；E2E **145 passed** | ✅（2 处数字偏差见 P-24） |
| CI 状态「未取得」 | `git ls-remote origin 'refs/heads/cch/12*'` → **空**（确无远端分支）⇒ 如实 | ✅ |
| 无跨票改动 / 无残留 / 无推送 | `git status --short` 空；`git diff --stat 33f0c872..c92bebb5` → 10 文件**全属本票面** | ✅ |

## 三、账本维度（A-036 逐条）

| A-036 需求 / 约束 | 实现证据 | 判定 |
|---|---|---|
| (a) 裁定强制点（写路径 or 读路径）并实现 | 裁定 **写路径**；`store:273` fail-closed；ADR-0011 成文 | ✅ |
| (b) 补齐替身**克隆保真度**使 S4 反映真实保证 | `structuredClone` 已补；A/B 的 B 变体复红证明旧绿系别名旁路 | ✅ |
| (c) 若裁定「读路径强制」须文档说明 | 裁定为写路径 ⇒ 条款未触发；仍立 ADR 留痕 | ✅ |
| 约束1 **不得以「保留假绿」方式回避** | **S4 现在反映真实保证**：① 断言区逐字节未变；② 变体 B（删强制+保真投递）复红 / 变体 C（删强制+按引用）复绿 ⇒ 二者只差投递保真度，直接证旧绿系别名旁路、新绿必须由写路径提供；③ `store:273` 位于 `push` 之前且其余三条写路径（改/删/`setExempt`）均不增长 `overrides`，门内无残留旁路 | ✅ **达成** |
| 约束2 不得放宽/删除既有断言 | 断言区 diff 为空；`verify-05` 100/100 与基线一致 | ✅ |
| 约束3 裁定须有据 | ADR-0011（15 来源表） | ✅ |
| 约束4 证据只认 CI run | 无远端写 ⇒ 无 CI；**未伪造**，如实登记 | ✅ |

**缺失 / 弱化 / 跑偏清单**：无。**残余（登记，非缺陷）**：`_writeRules` 自身不校验不变量（当前 4 个调用点全在 Store 内，不构成现实缺口）；S4 断言为 `<= 500`（禁改），无法区分「恰为 500」与「被截更少」，属断言固有粒度。

## 四、过程违规（单独呈报，**不替你追认**）

| # | 事项 | 实物证据 | 待裁定 |
|---|---|---|---|
| **P-23（新）** | 票 09 **修改了 `README.md`**（波次表 / 票务状态表 / 对账闸）—— 该文件属大脑维护面 | `git show --stat 5610de77` 含 `README.md \| 38 ++--`；报告 E-6 自曝，理由为 WORKFLOW §4.3/§7.3 将「波次表勾销」列为收口动作 | 是否追认（我倾向认可：WORKFLOW 授权） |
| **P-24（新）** | 票 12 报告 **2 处门计数 + 2 处行号与实物不符**：`verify-15` 报 36/36（实测 **28/28**）· `verify-03` 报 58/0（本机 **57/1**，见 P-25）· 行号 `:212`/`:268`（实测 `:220`/`:273`） | 我实跑 + `grep -n` | 是否要求补正表述 |
| **P-25（新）** | 票 09 报告记归档 handoff **11,102 B**，实测 **11,117 B**（15 B 漂移，写报告时先测后改） | `wc -lc` | 是否要求补正 |
| **P-26（新，方法级）** | **`verify-03` 的 G8c 是本机确定性红**（微基准：门控关时 trace 100000 次 < 50ms；实测 **63.71 / 71.03 / 65.88 ms**，空载连跑 3 次均红），而 **CI 上该门通过** ⇒ 机器性能差异，**非代码回归**。含义：**历史上各票报告与复核里「verify-03 = 58/0」的本地声明从未被本地复现**（含我 W5 的抽查也未捕获汇总行）——属「本地数字抄自报告」的**方法级隐患** | 我空载连跑 ×3 + `verify-03.yml` 在 CI success | 是否登记为方法级纪律（本地跑不过的门一律标注「以 CI 为准」） |
| **正面** | 票 09 **自曝 7 项偏离** + 3 条教训候选 + **R-1 完整归因链**（未掩盖）；票 12 **为裁定立 ADR** + 交付**可复现 A/B 脚本** + 如实声明「无 CI」而非伪造绿 | 两份报告 | — |

## 五、完成定义对照

| 票 | issue 勾销 + 各附 sha | 报告落位 | 版本控制 | 结论 |
|---|---|---|---|---|
| 09 | ✅ 四条（附只读证据） | ✅ | ✅（**零远端写**，经用户裁定「暂不推送」） | ✅ |
| 12 | ✅ 七条 | ✅ | ✅（零远端写，如实声明无 CI） | ✅ |

## 六、返工判定

**两票均无源码层面返工。** 所有偏差属**呈报数字口径**（P-24 / P-25）与**本地验证方法**（P-26），已登记。

## 七、frontier（重算）+ 下一波

| 项 | 状态 |
|---|---|
| **W6 = 票 09 · 12** | ✅ **全部复核通过** |
| **Cycle-6 实施面** | ✅ **12/12 完成并复核通过**（A-026…A-036 全 implemented；D-001…D-016 全落定） |
| **下一波 = land + 发版** | ⏳ **须用户逐次授权**（`land` 触发 `release.yml`，**不可逆**）。**三项前置**：① **R-1**：把 `cch/08` 重挂到 `cch/10` 终态之上（否则其 E2E 红随快照进入 main）；② **P-20**：多支本地 tip ≠ 远端 tip，推送前须对齐；③ **P-4**：票 04 issue 未勾销（实现已实物复核属实，建议 land 时由大脑补勾） |

**结论：无新的实施票可开 —— Cycle-6 实施面已闭合；下一个动作是 land/发版的授权决策。**
